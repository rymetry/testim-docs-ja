# ドキュメント更新日トラッキングについて

## 概要

このプロジェクトでは、英語原文の更新状況を追跡するために自動化スクリプトを使用しています。

## 重要な注意事項

### HTMLの `updatedAt` と表示される日付の乖離

英語原文サイト (help.testim.io) では、以下の2つの日付情報が存在します:

1. **HTMLメタデータの `updatedAt` フィールド**
   - HTMLソース内の JSON データに含まれる
   - 例: `"updatedAt":"2025-11-13T13:52:49.000Z"`
   - ページ構造やメタデータの更新日を示す可能性がある

2. **ページに表示される相対日付**
   - ユーザーに表示される日付
   - 例: "Updated about 2 months ago"
   - 実際のコンテンツ更新日を示す

### 現在の実装

自動化スクリプトは **HTMLの `updatedAt` フィールド** を使用しています。

これは技術的に取得可能な唯一の絶対日付ですが、以下の制限があります:

- ページ表示の相対日付と一致しない場合がある
- メタデータ更新とコンテンツ更新が別タイミングで行われる可能性がある

### 実運用での対応

#### ケース1: 日付が矛盾している場合

**症状:**
- HTMLの `updatedAt`: 2025-11-13
- 表示: "Updated about 2 months ago" (約2025-09-13)

**対応方法:**
1. 英語原文のコンテンツを実際に確認
2. 前回の翻訳時から実質的な変更があるか判断
3. 変更がない場合:
   - 日本語版の `updated` をそのまま維持
   - または表示される相対日付に合わせて調整
4. 変更がある場合:
   - 翻訳を更新
   - HTMLの `updatedAt` を使用

#### ケース2: 定期メンテナンス

定期（3日ごと）の GitHub Actions チェックでは:

1. HTMLの `updatedAt` で差分を検出
2. イシューが作成される
3. 担当者が英語原文を目視確認
4. 実質的な変更がある場合のみ翻訳更新
5. 変更内容に応じて `updated` フィールドを調整

## スクリプト使用方法

### 全ファイルの日付確認

```bash
npm run check:dates
```

### 更新が必要なファイルの検出

```bash
npm run check:updates
```

### 日付の一括更新

```bash
# ドライラン（確認のみ）
npm run update:dates

# 実際に更新
npm run update:dates:apply

# 特定フォルダのみ
npm run update:dates:apply -- --pattern recording-tests
```

## ベストプラクティス

1. **自動検出を信頼しすぎない**
   - HTMLの日付はあくまで参考値
   - 実際のコンテンツ変更を確認する

2. **段階的な更新**
   - 一度に全ファイルを更新せず、セクションごとに対応
   - 各更新で実際の変更内容を記録

3. **ドキュメント履歴の維持**
   - Git コミットメッセージに変更内容を記載
   - 必要に応じて CHANGELOG を作成

4. **相対日付の活用**
   - 表示される "Updated X months ago" も参考にする
   - 大きな乖離がある場合は要注意

## トラブルシューティング

### HTMLに `updatedAt` が見つからない

**原因:**
- ページ構造の変更
- 新しいドキュメント形式

**対応:**
- スクリプトの正規表現パターンを更新
- または手動で日付を設定

### 日付が頻繁に変わる

**原因:**
- 英語サイトで軽微なメタデータ更新が頻繁に発生

**対応:**
- 実質的なコンテンツ変更がない場合は日本語版の日付を維持
- `updated` フィールドを手動で管理

### GitHub Actions が失敗する

**原因:**
- ネットワークエラー
- help.testim.io のレート制限

**対応:**
- スクリプトにリトライロジックを追加
- レート制限を考慮した待機時間を設定

## 関連ファイル

- `scripts/check_outdated_docs.mjs` - 更新検出
- `scripts/fetch_all_updated_dates.mjs` - 日付一括取得
- `scripts/update_dates_from_english.mjs` - 日付自動更新
- `.github/workflows/check-docs-updates.yml` - 3日ごとの定期チェック
- `DOCS_REVIEW.md` - レビューガイドライン

---

最終更新: 2025-11-14
